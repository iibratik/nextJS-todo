export interface Field {
  size: number;
  name: string;
  label: string;
  type: string;
  autoComplete?: string;
  value: string;
  error: string;
}
export interface User {
  firstName: string,
  secondName: string,
  username: string,
  userId: string
}